# Runtime dependencies for the PenguWave backend.
# Auth-specific libs (password hashing, JWT) will be added in the auth task.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic-settings>=2.7
sqlmodel>=0.0.22
psycopg2-binary>=2.9
